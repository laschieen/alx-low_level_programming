#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *@int: the integer to be processed.
 *
 * Return: the absolute value of the int
 */
int _abs(int)
{
	if (int < 0)
	{
		return (-int);
	}

	return (int);
}
